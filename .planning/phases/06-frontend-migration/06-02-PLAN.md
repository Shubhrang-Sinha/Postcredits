---
phase: 06-frontend-migration
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - frontend/src/components/Header.tsx
  - frontend/src/components/MediaCard.tsx
  - frontend/src/components/GenreFilter.tsx
  - frontend/src/components/GenreFilterClient.tsx
autonomous: true
requirements:
  - UI-03
  - UI-05
  - UI-06

must_haves:
  truths:
    - "Header uses Tailwind classes with semantic tokens"
    - "MediaCard implements hover states and responsive grid"
    - "GenreFilter components use Tailwind styling"
    - "All components use interaction state system"
  artifacts:
    - path: "frontend/src/components/Header.tsx"
      provides: "Sticky header with backdrop blur, semantic tokens"
    - path: "frontend/src/components/MediaCard.tsx"
      provides: "Media card with hover, scale, shadow states"
    - path: "frontend/src/components/GenreFilter.tsx"
      provides: "Genre filter with pill buttons"
    - path: "frontend/src/components/GenreFilterClient.tsx"
      provides: "Client-side genre filter with active states"
  key_links:
    - from: "components/Header.tsx"
      to: "globals.css"
      via: "Uses semantic tokens: bg-primary/80, text-primary"
    - from: "components/MediaCard.tsx"
      to: "globals.css"
      via: "Uses hover:scale, shadow-lg, semantic colors"
---

<objective>
Migrate core UI components (Header, MediaCard, GenreFilter) to use Tailwind with semantic tokens.

Purpose: Update the most visible components to use the new design system.
Output: Header, MediaCard, GenreFilter components using Tailwind + semantic tokens.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-frontend-migration/06-UI-SPEC.md
@.planning/phases/06-frontend-migration/06-01-PLAN.md
@frontend/src/components/Header.tsx
@frontend/src/components/MediaCard.tsx
@frontend/src/components/GenreFilter.tsx
@frontend/src/components/GenreFilterClient.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Header component</name>
  <files>frontend/src/components/Header.tsx</files>
  <action>
Update Header.tsx to use semantic tokens:

Structure (per UI-SPEC):
- sticky, backdrop-blur
- Background: bg-primary/80 (using semantic token)
- Height: py-3 (12px vertical)
- Content: logo (left), nav links (right)
- Z-index: z-50

Remove any Radix Themes classes - use pure Tailwind.
Use semantic tokens: bg-primary, text-primary, text-secondary, accent.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | head -20</automated>
  </verify>
  <done>Header renders with semantic tokens, no Radix Themes</done>
</task>

<task type="auto">
  <name>Task 2: Migrate MediaCard component</name>
  <files>frontend/src/components/MediaCard.tsx</files>
  <action>
Update MediaCard.tsx:

Structure (per UI-SPEC):
- vertical stack
- Image: aspect-[2/3] object-cover
- Content: title, author/director, year, rating
- Hover: scale-105, shadow-lg
- Grid: responsive columns (handled by parent)

Use semantic tokens for:
- text-primary for titles
- text-secondary for metadata
- accent for ratings
- bg-secondary for card background

Add interaction states: hover:scale-105, hover:shadow-lg (per UI-SPEC)
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | head -20</automated>
  </verify>
  <done>MediaCard has hover states and semantic tokens</done>
</task>

<task type="auto">
  <name>Task 3: Migrate GenreFilter components</name>
  <files>frontend/src/components/GenreFilter.tsx, frontend/src/components/GenreFilterClient.tsx</files>
  <action>
Update both GenreFilter.tsx and GenreFilterClient.tsx:

Per UI-SPEC:
- Type: horizontal scroll on mobile, grid on desktop
- Item: pill-shaped button
- Active: bg-accent text-primary
- Inactive: bg-secondary text-secondary
- Hover states

Use semantic tokens:
- bg-secondary for inactive
- bg-accent (text-primary) for active
- text-secondary for inactive text

Remove any Radix Themes Button/SegmentedControl usage.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | head -20</automated>
  </verify>
  <done>GenreFilter uses pill buttons with correct active/inactive states</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | No security boundaries - styling refactoring |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|----------------|
| T-06-02 | - | Components | accept | Styling migration only, no security impact |
</threat_model>

<verification>
1. npm run build succeeds in frontend directory
2. All components render without @radix-ui/themes
3. Semantic tokens used throughout
</verification>

<success_criteria>
- Header uses Tailwind with semantic tokens, backdrop blur
- MediaCard implements hover:scale-105, hover:shadow-lg
- GenreFilter has active/inactive states with semantic tokens
</success_criteria>

<output>
After completion, create `.planning/phases/06-frontend-migration/06-02-SUMMARY.md`
</output>