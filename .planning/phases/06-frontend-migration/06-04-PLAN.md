---
phase: 06-frontend-migration
plan: 04
type: execute
wave: 4
depends_on:
  - 06-03
files_modified:
  - frontend/src/lib/auth-context.tsx
  - frontend/src/app/login/page.tsx
  - frontend/src/app/register/page.tsx
autonomous: false
requirements:
  - UI-07
  - UI-09

must_haves:
  truths:
    - "Auth context documents httpOnly cookie options"
    - "Form error handling implemented"
    - "Focus states visible on all interactive elements"
    - "Contrast ratios meet WCAG requirements"
  artifacts:
    - path: "frontend/src/lib/auth-context.tsx"
      provides: "Auth with localStorage, documented tradeoffs"
    - path: "frontend/src/app/login/page.tsx"
      provides: "Login with focus states, aria-labels"
    - path: "frontend/src/app/register/page.tsx"
      provides: "Register with accessibility"
  key_links:
    - from: "auth-context.tsx"
      to: "localStorage"
      via: "token storage with documented tradeoffs"
    - from: "login/page.tsx"
      to: "accessibility"
      via: "aria-labels, focus-visible, form validation"
---

<objective>
Improve authentication security documentation and verify accessibility requirements.

Purpose: Document httpOnly cookie tradeoffs per D-05/D-06, add focus states and accessibility.
Output: Auth context with documented tradeoffs, accessible forms.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-frontend-migration/06-UI-SPEC.md
@.planning/phases/06-frontend-migration/06-03-PLAN.md
@frontend/src/lib/auth-context.tsx
@frontend/src/app/login/page.tsx
@frontend/src/app/register/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document auth security tradeoffs</name>
  <files>frontend/src/lib/auth-context.tsx</files>
  <action>
Per D-05/D-06 and UI-SPEC:
- Document that httpOnly cookies require backend support
- Document that localStorage is currently used (XSS vulnerable)
- Document tradeoffs if httpOnly cannot be implemented
- Add comments about token expiration handling
- Keep localStorage for MVP compatibility per UI-SPEC

Add a detailed comment block explaining the security tradeoffs.
This documents the decision for future improvement.
</action>
<verify>
<automated>grep -q "httpOnly\|tradeoff\|XSS" frontend/src/lib/auth-context.tsx && echo "OK"</automated>
</verify>
<done>Auth context documents httpOnly cookie tradeoffs</done>
</task>

<task type="auto">
  <name>Task 2: Add accessibility to login page</name>
  <files>frontend/src/app/login/page.tsx</files>
  <action>
Per UI-SPEC Accessibility Requirements:
- Focus visibility: focus-visible ring on interactive elements
- Icon buttons: aria-label required
- Form labels: htmlFor association, visible labels
- Error messages: aria-live for dynamic content
- Contrast: text-primary on bg-primary

Update login/page.tsx:

1. Add focus-visible ring: focus-visible:ring-2 focus-visible:ring-accent
2. Add aria-labels to icon buttons if any
3. Ensure form inputs have visible labels (already have labels)
4. Add aria-live="polite" to error message container
5. Verify color contrast with semantic tokens

The page already uses semantic tokens - add focus states.
</action>
<verify>
<automated>cd frontend && npm run build 2>&1 | head -20</automated>
</verify>
<done>Login page has focus-visible states, aria attributes</done>
</task>

<task type="auto">
  <name>Task 3: Add accessibility to register page</name>
  <files>frontend/src/app/register/page.tsx</files>
  <action>
Same accessibility requirements as login page:
- Focus visibility: focus-visible ring
- Form labels: visible labels with htmlFor
- Error messages: aria-live
- Contrast: semantic tokens

Update register/page.tsx with same accessibility improvements.
</action>
<verify>
<automated>cd frontend && npm run build 2>&1 | head -20</automated>
</verify>
<done>Register page has focus-visible states, aria attributes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify migration visually</name>
  <action>Wait for human verification of the complete frontend migration. The agent has completed all automated tasks - this checkpoint requires user to visually verify the migration is successful.</action>
  <what-built>Migration verification - visual and functional check</what-built>
  <how-to-verify>
    1. Run: cd frontend && npm run dev
    2. Visit http://localhost:3000
    3. Check: Login page renders with Tailwind styling (no Radix Themes)
    4. Check: Register page renders correctly
    5. Check: Home page shows movies/books with new styling
    6. Check: Focus states visible when tabbing through elements
    7. Check: Colors use semantic tokens (accent green, etc.)
  </how-to-verify>
  <done>Human approves migration visual verification</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary   | Description                                          |
| ---------- | ---------------------------------------------------- |
| client→API | Auth token in localStorage, XSS risk documented      |
| None       | No new security threats - accessibility improvements |

## STRIDE Threat Register

| Threat ID | Category | Component    | Disposition | Mitigation Plan                               |
| --------- | -------- | ------------ | ----------- | --------------------------------------------- |
| T-06-04   | I        | Auth context | accept      | XSS risk documented in code comments per D-06 |

</threat_model>

<verification>
1. Code comments document httpOnly cookie tradeoffs
2. Focus-visible states added to forms
3. aria-live on error messages
4. Visual verification via checkpoint
</verification>

<success_criteria>

- Auth context documents localStorage tradeoffs per D-06
- Login/Register forms have focus-visible states
- All interactive elements have proper aria attributes
- Visual check passes with human verification
  </success_criteria>

<output>
After completion, create `.planning/phases/06-frontend-migration/06-04-SUMMARY.md`
</output>
