---
phase: 06-frontend-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/src/app/layout.tsx
  - frontend/src/app/globals.css
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-04
  - UI-05

must_haves:
  truths:
    - "@radix-ui/themes package fully removed from project"
    - "Tailwind config contains all semantic color tokens per UI-SPEC"
    - "Typography scale uses Tailwind theme extension"
    - "Layout rules implemented via Tailwind classes"
  artifacts:
    - path: "frontend/package.json"
      provides: "No @radix-ui/themes dependency"
    - path: "frontend/src/app/globals.css"
      provides: "Semantic color tokens in @theme"
    - path: "frontend/src/app/layout.tsx"
      provides: "No Theme wrapper, pure Tailwind"
  key_links:
    - from: "globals.css"
      to: "layout.tsx"
      via: "No @radix-ui/themes imports"
---

<objective>
Foundation for migration: Remove @radix-ui/themes dependency and configure semantic tokens in Tailwind CSS.

Purpose: Establish the design system foundation that all subsequent components will use.
Output: Project without @radix-ui/themes, Tailwind configured with semantic tokens.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-frontend-migration/06-CONTEXT.md
@.planning/phases/06-frontend-migration/06-UI-SPEC.md
@frontend/package.json
@frontend/src/app/globals.css
@frontend/src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove @radix-ui/themes dependency</name>
  <files>frontend/package.json</files>
  <action>
Remove @radix-ui/themes from dependencies in package.json using npm uninstall.
Then run npm install to update package-lock.json.
  </action>
  <verify>
    <automated>grep -q '"@radix-ui/themes"' frontend/package.json && echo "FAILED" || echo "OK"</automated>
  </verify>
  <done>@radix-ui/themes not in package.json dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Add semantic color tokens to Tailwind</name>
  <files>frontend/src/app/globals.css</files>
  <action>
Update globals.css @theme block to include all semantic tokens from 06-UI-SPEC.md:

Add to @theme:
- --color-bg-primary: #191111
- --color-bg-secondary: #261d1d
- --color-bg-tertiary: #312828
- --color-text-primary: #f0dfde
- --color-text-secondary: #bccbb9
- --color-border-subtle: #3d4a3d
- --color-accent: #53e076
- --color-accent-hover: #1db954
- --color-accent-active: #45a049
- --color-danger: #ef4444
- --color-danger-hover: #dc2626

Also add typography scale:
- --font-family: "Plus Jakarta Sans", system-ui, -apple-system, sans-serif

Keep existing CSS variables for compatibility but map to new semantic tokens in @theme.
  </action>
  <verify>
    <automated>grep -q "bg-primary" frontend/src/app/globals.css && grep -q "text-primary" frontend/src/app/globals.css && echo "OK"</automated>
  </verify>
  <done>globals.css contains all semantic tokens in @theme block</done>
</task>

<task type="auto">
  <name>Task 3: Remove Theme wrapper from layout.tsx</name>
  <files>frontend/src/app/layout.tsx</files>
  <action>
Remove the following from layout.tsx:
1. Import: import { Theme } from "@radix-ui/themes";
2. Import: import "@radix-ui/themes/styles.css";
3. Wrap: Remove <Theme> wrapper, keep only the children inside AuthProvider

The root html/body should remain with Tailwind classes only.
Per D-02: Use Tailwind CSS as single source of truth for styling.
  </action>
  <verify>
    <automated>grep -q "radix-ui" frontend/src/app/layout.tsx && echo "FAILED" || echo "OK"</automated>
  </verify>
  <done>layout.tsx has no @radix-ui/themes imports or components</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | No external input boundaries - this is a refactoring task |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|----------------|
| T-06-01 | - | This plan | accept | No security changes - styling migration only |
</threat_model>

<verification>
1. npm install succeeds in frontend directory
2. grep "@radix-ui/themes" package.json returns no matches
3. grep "@radix-ui" layout.tsx returns no matches
4. All semantic tokens present in globals.css @theme
</verification>

<success_criteria>
- @radix-ui/themes fully removed from package.json
- Tailwind @theme contains semantic color tokens per UI-SPEC
- layout.tsx has no Theme wrapper - pure Tailwind styling
</success_criteria>

<output>
After completion, create `.planning/phases/06-frontend-migration/06-01-SUMMARY.md`
</output>