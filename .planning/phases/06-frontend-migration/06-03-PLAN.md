---
phase: 06-frontend-migration
plan: 03
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - frontend/src/components/StarRating.tsx
  - frontend/src/components/RecommendationCarousel.tsx
  - frontend/src/components/StatsChart.tsx
  - frontend/src/lib/api.ts
autonomous: true
requirements:
  - UI-06
  - UI-08

must_haves:
  truths:
    - "StarRating is interactive with hover states"
    - "RecommendationCarousel has horizontal scroll, responsive"
    - "StatsChart uses accent palette colors"
    - "API uses NEXT_PUBLIC_API_URL with fallback"
  artifacts:
    - path: "frontend/src/components/StarRating.tsx"
      provides: "Interactive 5-star rating, filled/empty states"
    - path: "frontend/src/components/RecommendationCarousel.tsx"
      provides: "Horizontal scroll, item width w-48, responsive"
    - path: "frontend/src/components/StatsChart.tsx"
      provides: "Bar/pie charts with accent palette"
    - path: "frontend/src/lib/api.ts"
      provides: "API client with NEXT_PUBLIC_API_URL"
  key_links:
    - from: "components/RecommendationCarousel.tsx"
      to: "globals.css"
      via: "Uses w-48, gap, horizontal scroll with semantic tokens"
    - from: "lib/api.ts"
      to: "process.env"
      via: "Uses NEXT_PUBLIC_API_URL"
---

<objective>
Migrate complex interactive components and verify API configuration.

Purpose: Update carousel, chart, and rating components to use Tailwind with semantic tokens.
Output: All complex components migrated, API URL properly configured.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-frontend-migration/06-UI-SPEC.md
@.planning/phases/06-frontend-migration/06-02-PLAN.md
@frontend/src/components/StarRating.tsx
@frontend/src/components/RecommendationCarousel.tsx
@frontend/src/components/StatsChart.tsx
@frontend/src/lib/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate StarRating component</name>
  <files>frontend/src/components/StarRating.tsx</files>
  <action>
Update StarRating.tsx:

Per UI-SPEC:

- Type: 5-star interactive
- Icon: Material Symbols star/star-outline
- Filled: text-accent
- Empty: text-secondary
- Size: text-xl
- Hover: clickable with hover states

Use semantic tokens:

- text-accent for filled stars
- text-secondary for empty stars
- Hover effects on interactive stars
  </action>
  <verify>
  <automated>cd frontend && npm run build 2>&1 | head -20</automated>
  </verify>
  <done>StarRating uses semantic tokens, Material Symbols</done>
  </task>

<task type="auto">
  <name>Task 2: Migrate RecommendationCarousel component</name>
  <files>frontend/src/components/RecommendationCarousel.tsx</files>
  <action>
Update RecommendationCarousel.tsx:

Per UI-SPEC:

- Type: horizontal scroll
- Item width: w-48 (192px)
- Gap: space-md
- Arrow navigation: on desktop only

Use semantic tokens:

- bg-primary for container
- text-primary for titles
- scrollbar styling if needed
- Responsive: hide arrows on mobile

Note: This component likely already uses Tailwind - verify and update any remaining non-semantic colors.
</action>
<verify>
<automated>cd frontend && npm run build 2>&1 | head -20</automated>
</verify>
<done>RecommendationCarousel has horizontal scroll, w-48 items</done>
</task>

<task type="auto">
  <name>Task 3: Migrate StatsChart component</name>
  <files>frontend/src/components/StatsChart.tsx</files>
  <action>
Update StatsChart.tsx:

Per UI-SPEC:

- Type: bar/pie charts via library
- Colors: accent palette
- Labels: text-secondary

Use semantic tokens:

- accent variants for chart bars/slices
- text-secondary for labels
- Verify the library being used and update colors accordingly

Note: Check what charting library is used - update to use semantic color tokens where possible.
</action>
<verify>
<automated>cd frontend && npm run build 2>&1 | head -20</automated>
</verify>
<done>StatsChart uses accent palette colors</done>
</task>

<task type="auto">
  <name>Task 4: Verify API URL configuration</name>
  <files>frontend/src/lib/api.ts</files>
  <action>
Per UI-SPEC and D-07/D-08:
- Client: NEXT_PUBLIC_API_URL env var with default http://localhost:3000
- Server: Internal Docker URL http://backend:3000 (fallback)

Check api.ts:

1. Ensure it uses process.env.NEXT_PUBLIC_API_URL
2. Default should be http://localhost:3000 for dev
3. Add comment about Docker production URL

Per D-08: Document that production server-side calls use internal Docker URL.
</action>
<verify>
<automated>grep -q "NEXT_PUBLIC_API_URL" frontend/src/lib/api.ts && echo "OK"</automated>
</verify>
<done>api.ts uses NEXT_PUBLIC_API_URL with proper fallback</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary   | Description                                 |
| ---------- | ------------------------------------------- |
| client→API | Untrusted API responses parsed and rendered |

## STRIDE Threat Register

| Threat ID | Category | Component       | Disposition | Mitigation Plan                                                |
| --------- | -------- | --------------- | ----------- | -------------------------------------------------------------- |
| T-06-03   | I        | StatsChart, API | accept      | Chart library sanitizes data, API responses handled by backend |

</threat_model>

<verification>
1. npm run build succeeds
2. All components use semantic tokens
3. API URL configured per D-07/D-08
</verification>

<success_criteria>

- StarRating: interactive, filled=accent, empty=secondary
- RecommendationCarousel: horizontal scroll, w-48 items
- StatsChart: accent palette for charts
- API: NEXT_PUBLIC_API_URL configured
  </success_criteria>

<output>
After completion, create `.planning/phases/06-frontend-migration/06-03-SUMMARY.md`
</output>
