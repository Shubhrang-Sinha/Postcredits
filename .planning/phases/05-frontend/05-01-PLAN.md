---
phase: 05-frontend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: []

must_haves:
  truths:
    - "Next.js project initialized with App Router"
    - "Tailwind CSS configured and working"
    - "@radix-ui/themes installed and configured"
    - "Project structure follows Next.js 14 App Router conventions"
  artifacts:
    - path: "frontend/package.json"
      provides: "Project dependencies and scripts"
    - path: "frontend/next.config.js"
      provides: "Next.js configuration"
    - path: "frontend/tailwind.config.ts"
      provides: "Tailwind CSS configuration"
    - path: "frontend/app/layout.tsx"
      provides: "Root layout with Radix Themes provider"
    - path: "frontend/app/page.tsx"
      provides: "Home page placeholder"
  key_links:
    - from: "frontend/app/layout.tsx"
      to: "@radix-ui/themes"
      via: "import and Provider component"
      pattern: "import.*Theme"
---

<objective>
Initialize Next.js 14 project with Tailwind CSS and Radix Themes. Set up the foundational project structure for the frontend application.
</objective>

<context>
@.planning/phases/05-frontend/05-CONTEXT.md
@.planning/frontend-integration.md
@.planning/ROADMAP.md

# Key decisions from CONTEXT.md:
- D-01: Framework — Next.js 14 (App Router, not Pages Router)
- D-02: Styling — Tailwind CSS + @radix-ui/themes component library
- D-03: Data Fetching — Server Components with fetch (not React Query/SWR)
- D-13: Use Next.js app directory structure
- D-17: Use @radix-ui/themes components

# Tech stack requirements:
- Next.js 14 with App Router
- Tailwind CSS v3+
- @radix-ui/themes
- TypeScript
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js project</name>
  <files>frontend/package.json, frontend/next.config.js, frontend/tsconfig.json</files>
  <read_first>
    - .planning/phases/05-frontend/05-CONTEXT.md (D-01, D-02 for tech stack decisions)
  </read_first>
  <action>
Create Next.js 14 project with TypeScript using create-next-app:

```bash
cd /home/saury/Projects/Postcredits
npx create-next-app@latest frontend --typescript --tailwind --eslint --app --src-dir --no-import-alias --use-npm
```

After initialization, install @radix-ui/themes:
```bash
cd frontend
npm install @radix-ui/themes
```

Configure next.config.js with minimal settings for now.
  </action>
  <acceptance_criteria>
- frontend/package.json exists with next: ^14.0.0
- frontend/next.config.js exists
- frontend/tsconfig.json exists
- @radix-ui/themes is in dependencies
  </acceptance_criteria>
  <verify>
    <automated>ls -la frontend/package.json frontend/next.config.js && cat frontend/package.json | grep -E '"next"|"@radix-ui/themes"'</automated>
  </verify>
  <done>Next.js 14 project initialized with package.json, next.config.js, tsconfig.json</done>
</task>

<task type="auto">
  <name>Task 2: Configure Tailwind CSS</name>
  <files>frontend/tailwind.config.ts, frontend/app/globals.css</files>
  <read_first>
    - frontend/tailwind.config.ts (created in Task 1)
  </read_first>
  <action>
Ensure Tailwind is properly configured. Verify/update tailwind.config.ts to include:
- content paths for app directory
- Extend theme with custom colors if needed

Update app/globals.css to include Tailwind directives:
```css
@tailwind base;
@tailwind components;
@tailwind utilities;
```
  </action>
  <acceptance_criteria>
- tailwind.config.ts exists and is valid
- app/globals.css contains @tailwind directives
- PostCSS is configured (usually via create-next-app)
  </acceptance_criteria>
  <verify>
    <automated>cat frontend/tailwind.config.ts | head -20</automated>
  </verify>
  <done>Tailwind CSS configured and working</done>
</task>

<task type="auto">
  <name>Task 3: Create root layout with Radix Themes</name>
  <files>frontend/app/layout.tsx, frontend/app/globals.css</files>
  <read_first>
    - .planning/phases/05-frontend/05-CONTEXT.md (D-17: Use @radix-ui/themes components)
    - https://www.radix-ui.com/themes/docs/overview/getting-started (Radix Themes docs)
  </read_first>
  <action>
Create frontend/app/layout.tsx with Radix Themes Provider:

```typescript
import { Theme } from '@radix-ui/themes';
import '@radix-ui/themes/styles.css';
import './globals.css';

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body>
        <Theme accentColor="cyan" grayColor="sand" radius="medium">
          {children}
        </Theme>
      </body>
    </html>
  );
}
```
  </action>
  <acceptance_criteria>
- frontend/app/layout.tsx exists
- Imports @radix-ui/themes and Theme component
- Wraps children in Theme provider
- Includes globals.css
  </acceptance_criteria>
  <verify>
    <automated>cat frontend/app/layout.tsx</automated>
  </verify>
  <done>Root layout with Radix Themes Provider created</done>
</task>

<task type="auto">
  <name>Task 4: Create home page placeholder</name>
  <files>frontend/app/page.tsx</files>
  <read_first>
    - frontend/app/layout.tsx (created in Task 3)
  </read_first>
  <action>
Create frontend/app/page.tsx with a basic placeholder:

```typescript
export default function HomePage() {
  return (
    <main className="p-8">
      <h1 className="text-3xl font-bold">Postcredits</h1>
      <p className="mt-4 text-gray-600">Welcome to your media discovery app</p>
    </main>
  );
}
```
  </action>
  <acceptance_criteria>
- frontend/app/page.tsx exists
- Exports default HomePage component
- Contains basic heading and content
  </acceptance_criteria>
  <verify>
    <automated>cat frontend/app/page.tsx</automated>
  </verify>
  <done>Home page placeholder created</done>
</task>

<task type="auto">
  <name>Task 5: Verify build works</name>
  <files>frontend</files>
  <read_first>
    - frontend/package.json (verify scripts exist)
  </read_first>
  <action>
Run the Next.js build to verify the project compiles:

```bash
cd frontend
npm run build
```

This should complete without errors.
  </action>
  <acceptance_criteria>
- npm run build completes successfully
- No TypeScript errors
- No lint errors
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Next.js project builds successfully</done>
</task>

</tasks>

<verification>
- [ ] package.json has next and @radix-ui/themes dependencies
- [ ] tailwind.config.ts is valid
- [ ] layout.tsx has Theme provider
- [ ] page.tsx renders
- [ ] npm run build passes
</verification>

<success_criteria>
Next.js 14 project initialized with Tailwind CSS and Radix Themes. Build passes. Ready for auth implementation.
</success_criteria>

<output>
After completion, create `.planning/phases/05-frontend/05-01-SUMMARY.md`
</output>